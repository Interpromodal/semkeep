{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Grep",
        "hooks": [
          {
            "type": "command",
            "command": "node",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/grep-nudge.mjs"],
            "statusMessage": "semkeep nudge",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
